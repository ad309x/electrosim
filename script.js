const canvas = document.getElementById("fieldCanvas"); const ctx = canvas.getContext("2d"); function resizeCanvas() { canvas.width = window.innerWidth - 200; canvas.height = window.innerHeight; drawScene(); } window.addEventListener("resize", resizeCanvas); let charges = []; let showField = true; let showEquip = true; let showPotentialMap = false; let equipDensity = 4; let fieldDensity = 4; let pixelsPerMeter = 100; const defaultChargeMagnitudeC = 1e-6; let nextChargeId = 1; let showChargeValues = false; let probeMode = false; let probes = []; let nextProbeId = 1; resizeCanvas(); document.querySelectorAll(".particle").forEach((item) => { item.addEventListener("dragstart", (e) => { const isProton = item.classList.contains("proton"); e.dataTransfer.setData("chargeType", isProton ? "proton" : "electron"); const dragIcon = document.createElement("canvas"); dragIcon.width = 40; dragIcon.height = 40; const ctx = dragIcon.getContext("2d"); ctx.beginPath(); ctx.arc(20, 20, 14, 0, Math.PI * 2); ctx.fillStyle = isProton ? "#ff4444" : "#4488ff"; ctx.fill(); ctx.closePath(); ctx.fillStyle = "white"; ctx.font = "bold 22px Arial"; ctx.textAlign = "center"; ctx.textBaseline = "middle"; ctx.fillText(isProton ? "+" : "−", 20, 21); dragIcon.style.position = "absolute"; dragIcon.style.top = "-100px"; dragIcon.style.left = "-100px"; document.body.appendChild(dragIcon); e.dataTransfer.setDragImage(dragIcon, 20, 20); setTimeout(() => document.body.removeChild(dragIcon), 0); }); }); canvas.addEventListener("dragover", (e) => e.preventDefault()); canvas.addEventListener("drop", (e) => { e.preventDefault(); const rect = canvas.getBoundingClientRect(); const dropX = e.clientX - rect.left; const dropY = e.clientY - rect.top; const type = e.dataTransfer.getData("chargeType"); const chargeSign = type === "proton" ? +1 : -1; const id = nextChargeId++; const label = `q${id}`; charges.push({ id, label, x: dropX, y: dropY, q: chargeSign, magnitudeC: defaultChargeMagnitudeC, }); updateHudCharges(); updateSimStats(); drawScene(); }); function drawScene() { ctx.fillStyle = "black"; ctx.fillRect(0, 0, canvas.width, canvas.height); if (showPotentialMap && charges.length > 0) { drawPotentialMap(); } if (showEquip && charges.length > 0) { drawEquipotentials(); } if (showField && charges.length > 0) { drawFieldLines(); } for (const c of charges) { ctx.beginPath(); ctx.arc(c.x, c.y, 14, 0, Math.PI * 2); ctx.fillStyle = c.q > 0 ? "#ff4444" : "#4488ff"; ctx.fill(); ctx.closePath(); ctx.fillStyle = "white"; ctx.font = "bold 16px Arial"; ctx.textAlign = "center"; ctx.textBaseline = "middle"; const chargeText = showChargeValues ? formatChargeValue(c.q * (c.magnitudeC ?? defaultChargeMagnitudeC)) : c.label; ctx.fillText(chargeText, c.x, c.y); } for (const p of probes) { drawProbe(p); } } function formatChargeValue(coulombs) { const sign = coulombs >= 0 ? "+" : "−"; const a = Math.abs(coulombs); let val, unit; if (a >= 1) { val = a; unit = "C"; } else if (a >= 1e-3) { val = a / 1e-3; unit = "mC"; } else if (a >= 1e-6) { val = a / 1e-6; unit = "μC"; } else if (a >= 1e-9) { val = a / 1e-9; unit = "nC"; } else { val = a; unit = "C"; } const text = val >= 100 ? val.toFixed(0) : val >= 10 ? val.toFixed(1) : val >= 1 ? val.toFixed(2) : val.toPrecision(2); return `${sign}${text} ${unit}`; } function updateAllControlDisplays() { const toggleFieldEl = document.getElementById("toggleField"); const toggleEquipEl = document.getElementById("toggleEquip"); const togglePotentialEl = document.getElementById("togglePotential"); const toggleChargeValuesEl = document.getElementById("toggleChargeValues"); if (toggleFieldEl) toggleFieldEl.checked = showField; if (toggleEquipEl) toggleEquipEl.checked = showEquip; if (togglePotentialEl) togglePotentialEl.checked = showPotentialMap; if (toggleChargeValuesEl) toggleChargeValuesEl.checked = showChargeValues; const pxInput = document.getElementById("pxPerMeterInput"); const scaleInfo = document.getElementById("scaleInfo"); if (pxInput) pxInput.value = String(pixelsPerMeter); if (scaleInfo) scaleInfo.textContent = `${pixelsPerMeter} pixels = 1 meter`; const fieldSlider = document.getElementById("fieldDensity"); const fieldValue = document.getElementById("fieldDensityVal"); if (fieldSlider) fieldSlider.value = String(fieldDensity); if (fieldValue) fieldValue.textContent = String(fieldDensity); const equipSlider = document.getElementById("equipDensity"); const equipValue = document.getElementById("equipDensityVal"); if (equipSlider) equipSlider.value = String(equipDensity); if (equipValue) equipValue.textContent = String(equipDensity); } function updateHudCharges() { const box = document.getElementById("hudCharges"); if (!box) return; if (charges.length === 0) { box.innerHTML = ""; return; } const rows = charges .slice() .sort((a, b) => a.id - b.id) .map((c) => { const valueC = c.q * (c.magnitudeC ?? defaultChargeMagnitudeC); return `<div class="row">Charge of ${c.label}: ${formatChargeValue(valueC)}</div>`; }) .join(""); box.innerHTML = rows; } function updateSimStats() { const stats = document.getElementById("simStats"); if (!stats) return; stats.innerHTML = `Charges: ${charges.length}<br>` + `Probes: ${probes.length}`; } const clearBtn = document.getElementById("clearBtn"); const clearProbesBtn = document.getElementById("clearProbesBtn"); const clearAllBtn = document.getElementById("clearAllBtn"); let clearMode = "charges"; const clearConfirmWindow = document.getElementById("clearConfirmWindow"); const confirmClearYes = document.getElementById("confirmClearYes"); const confirmClearNo = document.getElementById("confirmClearNo"); const clearConfirmTitle = document.getElementById("clearConfirmTitle"); const clearConfirmMessage = document.getElementById("clearConfirmMessage"); const clearProbesConfirmWindow = document.getElementById("clearProbesConfirmWindow"); const confirmClearProbesYes = document.getElementById("confirmClearProbesYes"); const confirmClearProbesNo = document.getElementById("confirmClearProbesNo"); if (clearBtn && clearConfirmWindow) { clearBtn.addEventListener("click", () => { clearMode = "charges"; clearConfirmTitle.textContent = "Clear Charges?"; clearConfirmMessage.textContent = "Are you sure you want to remove all charges?"; clearConfirmWindow.classList.remove("hidden"); }); } if (clearProbesBtn && clearProbesConfirmWindow) { clearProbesBtn.addEventListener("click", () => { clearProbesConfirmWindow.classList.remove("hidden"); }); } if (clearAllBtn && clearConfirmWindow) { clearAllBtn.addEventListener("click", () => { clearMode = "all"; clearConfirmTitle.textContent = "Clear All?"; clearConfirmMessage.textContent = "Are you sure you want to remove everything?"; clearConfirmWindow.classList.remove("hidden"); }); } if (confirmClearYes && clearConfirmWindow) { confirmClearYes.addEventListener("click", () => { charges = []; if (clearMode === "all") { probes = []; nextProbeId = 1; } updateHudCharges(); updateSimStats(); const display = document.getElementById("currentScenarioDisplay"); if (display) { display.textContent = "Current Configuration: None"; } drawScene(); clearConfirmWindow.classList.add("hidden"); }); } if (confirmClearNo && clearConfirmWindow) { confirmClearNo.addEventListener("click", () => { clearConfirmWindow.classList.add("hidden"); }); } if (confirmClearProbesYes && clearProbesConfirmWindow) { confirmClearProbesYes.addEventListener("click", () => { probes = []; nextProbeId = 1; updateSimStats(); drawScene(); clearProbesConfirmWindow.classList.add("hidden"); }); } if (confirmClearProbesNo && clearProbesConfirmWindow) { confirmClearProbesNo.addEventListener("click", () => { clearProbesConfirmWindow.classList.add("hidden"); }); } if (clearProbesConfirmWindow) { clearProbesConfirmWindow.addEventListener("click", (e) => { if (e.target === clearProbesConfirmWindow) { clearProbesConfirmWindow.classList.add("hidden"); } }); } if (clearConfirmWindow) { clearConfirmWindow.addEventListener("click", (e) => { if (e.target === clearConfirmWindow) { clearConfirmWindow.classList.add("hidden"); } }); } function createSetupCode() { const setup = { version: 1, charges: charges.map((c) => ({ xRatio: c.x / canvas.width, yRatio: c.y / canvas.height, q: c.q, magnitudeC: c.magnitudeC ?? defaultChargeMagnitudeC })), probes: probes.map((p) => ({ xRatio: p.x / canvas.width, yRatio: p.y / canvas.height })), settings: { showField, showEquip, showPotentialMap, showChargeValues, pixelsPerMeter, fieldDensity, equipDensity } }; return "EMSIM:" + btoa(JSON.stringify(setup)); } function loadSetupFromCode(code) { let cleaned = code.trim(); if (cleaned.startsWith("EMSIM:")) { cleaned = cleaned.slice(6); } const setup = JSON.parse(atob(cleaned)); if (!setup || setup.version !== 1) { throw new Error("Unsupported setup version."); } if (!Array.isArray(setup.charges)) { throw new Error("Missing charges."); } charges = []; probes = []; nextChargeId = 1; nextProbeId = 1; for (const savedCharge of setup.charges) { const id = nextChargeId++; charges.push({ id, label: `q${id}`, x: savedCharge.xRatio * canvas.width, y: savedCharge.yRatio * canvas.height, q: savedCharge.q === -1 ? -1 : 1, magnitudeC: typeof savedCharge.magnitudeC === "number" ? savedCharge.magnitudeC : defaultChargeMagnitudeC }); } if (Array.isArray(setup.probes)) { for (const savedProbe of setup.probes) { const id = nextProbeId++; probes.push({ id, label: `P${id}`, x: savedProbe.xRatio * canvas.width, y: savedProbe.yRatio * canvas.height }); } } if (setup.settings) { showField = setup.settings.showField !== false; showEquip = setup.settings.showEquip !== false; showPotentialMap = setup.settings.showPotentialMap === true; showChargeValues = setup.settings.showChargeValues === true; pixelsPerMeter = Number.isInteger(setup.settings.pixelsPerMeter) && setup.settings.pixelsPerMeter >= 1 && setup.settings.pixelsPerMeter <= 500 ? setup.settings.pixelsPerMeter : 100; fieldDensity = Number.isInteger(setup.settings.fieldDensity) && setup.settings.fieldDensity >= 1 && setup.settings.fieldDensity <= 10 ? setup.settings.fieldDensity : 4; equipDensity = Number.isInteger(setup.settings.equipDensity) && setup.settings.equipDensity >= 1 && setup.settings.equipDensity <= 10 ? setup.settings.equipDensity : 4; } const display = document.getElementById("currentScenarioDisplay"); if (display) { display.textContent = "Current Configuration: Loaded Saved Setup"; } updateAllControlDisplays(); updateHudCharges(); updateSimStats(); drawScene(); } const saveSetupBtn = document.getElementById("saveSetupBtn"); const loadSetupBtn = document.getElementById("loadSetupBtn"); const saveSetupWindow = document.getElementById("saveSetupWindow"); const loadSetupWindow = document.getElementById("loadSetupWindow"); const closeSaveSetupBtn = document.getElementById("closeSaveSetupBtn"); const closeLoadSetupBtn = document.getElementById("closeLoadSetupBtn"); const saveSetupCode = document.getElementById("saveSetupCode"); const loadSetupCode = document.getElementById("loadSetupCode"); const copySetupCodeBtn = document.getElementById("copySetupCodeBtn"); const confirmLoadSetupBtn = document.getElementById("confirmLoadSetupBtn"); const copySetupStatus = document.getElementById("copySetupStatus"); const loadSetupError = document.getElementById("loadSetupError"); if (saveSetupBtn && saveSetupWindow && saveSetupCode) { saveSetupBtn.addEventListener("click", () => { saveSetupCode.value = createSetupCode(); if (copySetupStatus) copySetupStatus.textContent = ""; saveSetupWindow.classList.remove("hidden"); saveSetupCode.focus(); saveSetupCode.select(); }); } if (loadSetupBtn && loadSetupWindow) { loadSetupBtn.addEventListener("click", () => { if (loadSetupCode) loadSetupCode.value = ""; if (loadSetupError) loadSetupError.textContent = ""; loadSetupWindow.classList.remove("hidden"); if (loadSetupCode) loadSetupCode.focus(); }); } if (closeSaveSetupBtn && saveSetupWindow) { closeSaveSetupBtn.addEventListener("click", () => { saveSetupWindow.classList.add("hidden"); }); } if (closeLoadSetupBtn && loadSetupWindow) { closeLoadSetupBtn.addEventListener("click", () => { loadSetupWindow.classList.add("hidden"); }); } if (saveSetupWindow) { saveSetupWindow.addEventListener("click", (e) => { if (e.target === saveSetupWindow) { saveSetupWindow.classList.add("hidden"); } }); } if (loadSetupWindow) { loadSetupWindow.addEventListener("click", (e) => { if (e.target === loadSetupWindow) { loadSetupWindow.classList.add("hidden"); } }); } if (copySetupCodeBtn && saveSetupCode) { copySetupCodeBtn.addEventListener("click", async () => { try { await navigator.clipboard.writeText(saveSetupCode.value); if (copySetupStatus) copySetupStatus.textContent = "Copied!"; } catch { saveSetupCode.focus(); saveSetupCode.select(); if (copySetupStatus) { copySetupStatus.textContent = "Select the code and copy it manually."; } } }); } if (confirmLoadSetupBtn && loadSetupCode && loadSetupWindow) { confirmLoadSetupBtn.addEventListener("click", () => { try { loadSetupFromCode(loadSetupCode.value); loadSetupWindow.classList.add("hidden"); } catch { if (loadSetupError) { loadSetupError.textContent = "Invalid setup code. Please check that you copied the full code."; } } }); } const screenshotBtn = document.getElementById("screenshotBtn"); screenshotBtn.addEventListener("click", () => { const hud = document.getElementById("hud"); if (hud) hud.style.display = "none"; setTimeout(() => { const link = document.createElement("a"); link.download = "image.png"; link.href = canvas.toDataURL("image/png"); link.click(); if (hud) hud.style.display = "block"; }, 100); }); const probeToolBtn = document.getElementById("probeToolBtn"); const resetDefaultsBtn = document.getElementById("resetDefaultsBtn"); if (resetDefaultsBtn) { resetDefaultsBtn.addEventListener("click", () => { showField = true; showEquip = true; showPotentialMap = false; showChargeValues = false; pixelsPerMeter = 100; fieldDensity = 4; equipDensity = 4; probeMode = false; if (probeToolBtn) { probeToolBtn.textContent = "Probe Tool: Off"; probeToolBtn.classList.remove("active"); } updateAllControlDisplays(); drawScene(); }); } if (probeToolBtn) { probeToolBtn.addEventListener("click", () => { probeMode = !probeMode; probeToolBtn.textContent = probeMode ? "Probe Tool: On" : "Probe Tool: Off"; probeToolBtn.classList.toggle("active", probeMode); }); } let draggingCharge = null; let isDragging = false; canvas.addEventListener("mousedown", (e) => { const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; if (probeMode && e.button === 0) { probes.push({ id: nextProbeId, label: `P${nextProbeId}`, x, y }); nextProbeId++; updateSimStats(); drawScene(); return; } for (const c of charges) { const dx = x - c.x; const dy = y - c.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 15) { draggingCharge = c; isDragging = true; break; } } }); canvas.addEventListener("mousemove", (e) => { if (!isDragging || !draggingCharge) return; const rect = canvas.getBoundingClientRect(); draggingCharge.x = e.clientX - rect.left; draggingCharge.y = e.clientY - rect.top; drawScene(); }); canvas.addEventListener("mouseup", () => { isDragging = false; draggingCharge = null; }); canvas.addEventListener("dblclick", (e) => { if (isDragging) return; const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; for (let i = charges.length - 1; i >= 0; i--) { const c = charges[i]; const dx = x - c.x; const dy = y - c.y; const dist = Math.hypot(dx, dy); if (dist < 15) { openChargeModal(c); break; } } }); canvas.addEventListener("contextmenu", (e) => { e.preventDefault(); const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; for (let i = probes.length - 1; i >= 0; i--) { const p = probes[i]; const dist = Math.hypot(x - p.x, y - p.y); if (dist < 12) { probes.splice(i, 1); updateSimStats(); drawScene(); return; } } for (let i = 0; i < charges.length; i++) { const c = charges[i]; const dx = x - c.x; const dy = y - c.y; const dist = Math.sqrt(dx * dx + dy * dy); if (dist < 15) { charges.splice(i, 1); updateHudCharges(); updateSimStats(); drawScene(); break; } } }); function computeElectricField(x, y) { let Ex = 0, Ey = 0; const k = 8.99e9; for (const c of charges) { const dx_px = x - c.x; const dy_px = y - c.y; const dx = dx_px / pixelsPerMeter; const dy = dy_px / pixelsPerMeter; const r2 = dx*dx + dy*dy; if (r2 === 0) continue; const r = Math.sqrt(r2); const qC = c.q * (c.magnitudeC ?? defaultChargeMagnitudeC); const E_mag = (k * qC) / r2; Ex += E_mag * (dx / r); Ey += E_mag * (dy / r); } return { Ex, Ey }; } function computePotential(x, y) { const k = 8.99e9; let V = 0; for (const c of charges) { const dx_px = x - c.x; const dy_px = y - c.y; const dx = dx_px / pixelsPerMeter; const dy = dy_px / pixelsPerMeter; const r = Math.hypot(dx, dy); if (r === 0) continue; const qC = c.q * (c.magnitudeC ?? defaultChargeMagnitudeC); V += (k * qC) / r; } return V; } function formatNumberForProbe(value, unit) { if (Math.abs(value) >= 1e6) { return `${(value / 1e6).toFixed(2)} ×10^6 ${unit}`; } else if (Math.abs(value) >= 1e3) { return `${(value / 1e3).toFixed(2)} ×10^3 ${unit}`; } else if (Math.abs(value) >= 1) { return `${value.toFixed(2)} ${unit}`; } else { return `${value.toExponential(2)} ${unit}`; } } function drawProbe(p) { const V = computePotential(p.x, p.y); const { Ex, Ey } = computeElectricField(p.x, p.y); const Emag = Math.hypot(Ex, Ey); let angleDeg = Math.atan2(-Ey, Ex) * 180 / Math.PI; if (angleDeg < 0) { angleDeg += 360; } ctx.beginPath(); ctx.arc(p.x, p.y, 6, 0, Math.PI * 2); ctx.fillStyle = "#00ff99"; ctx.fill(); ctx.closePath(); ctx.strokeStyle = "#ffffff"; ctx.lineWidth = 1; ctx.stroke(); ctx.fillStyle = "#ffffff"; ctx.font = "bold 12px Arial"; ctx.textAlign = "left"; ctx.textBaseline = "top"; const x_m = (p.x / pixelsPerMeter).toFixed(2); const y_m = (p.y / pixelsPerMeter).toFixed(2); ctx.fillText( `${p.label}: (${x_m}, ${y_m}) m`, p.x + 10, p.y - 28 ); ctx.fillText( `V=${formatNumberForProbe(V, "V")}`, p.x + 10, p.y - 14 ); ctx.fillText( `|E|=${formatNumberForProbe(Emag, "N/C")}, θ=${angleDeg.toFixed(1)}°`, p.x + 10, p.y ); } function traceFieldLine(x, y, step = 8, maxSteps = 2000) { const points = []; let px = x; let py = y; for (let i = 0; i < maxSteps; i++) { const { Ex, Ey } = computeElectricField(px, py); const E = Math.sqrt(Ex * Ex + Ey * Ey); if (E < 0.0001) break; const nx = Ex / E; const ny = Ey / E; px += nx * step; py += ny * step; if (px < 0 || py < 0 || px > canvas.width || py > canvas.height) break; points.push({ x: px, y: py }); } return points; } function drawFieldLines() { if (charges.length === 0) return; const numSeeds = Math.max(6, Math.round(6 + fieldDensity * 2.5)); const lineColor = "rgba(255, 180, 80, 0.7)"; for (const c of charges) { for (let i = 0; i < numSeeds; i++) { const angle = (i / numSeeds) * 2 * Math.PI; const startX = c.x + 20 * Math.cos(angle); const startY = c.y + 20 * Math.sin(angle); const forward = traceFieldLine(startX, startY, 8); const backward = traceFieldLine(startX, startY, -8); const linePoints = backward.reverse().concat(forward); ctx.beginPath(); if (linePoints.length > 0) ctx.moveTo(linePoints[0].x, linePoints[0].y); for (const p of linePoints) ctx.lineTo(p.x, p.y); ctx.strokeStyle = lineColor; ctx.lineWidth = 1; ctx.stroke(); drawArrowsAlongLine(linePoints, 100); } } } function drawArrowsAlongLine(points, spacing = 100) { if (points.length < 2) return; let distance = 0; for (let i = 1; i < points.length; i++) { const dx = points[i].x - points[i - 1].x; const dy = points[i].y - points[i - 1].y; const segLength = Math.sqrt(dx * dx + dy * dy); distance += segLength; if (distance > spacing) { distance = 0; const angle = Math.atan2(dy, dx); const size = 6; ctx.beginPath(); ctx.moveTo(points[i].x, points[i].y); ctx.lineTo(points[i].x - size * Math.cos(angle - Math.PI / 6), points[i].y - size * Math.sin(angle - Math.PI / 6)); ctx.moveTo(points[i].x, points[i].y); ctx.lineTo(points[i].x - size * Math.cos(angle + Math.PI / 6), points[i].y - size * Math.sin(angle + Math.PI / 6)); ctx.strokeStyle = "rgba(255,255,255,0.8)"; ctx.lineWidth = 1; ctx.stroke(); } } } function traceEquipotential(x, y, step = 10, maxSteps = 1200) { const forward = _traceIso(x, y, +step, maxSteps); const backward = _traceIso(x, y, -step, maxSteps); return backward.reverse().concat(forward); } function _traceIso(x, y, step, maxSteps) { const pts = []; let px = x, py = y; for (let i = 0; i < maxSteps; i++) { const { Ex, Ey } = computeElectricField(px, py); const mag = Math.hypot(Ex, Ey); if (mag < 1e-3) break; const nx = -Ey / mag; const ny = +Ex / mag; px += nx * step; py += ny * step; if (px < 0 || py < 0 || px > canvas.width || py > canvas.height) break; if (_nearAnyCharge(px, py, 18)) break; pts.push({ x: px, y: py }); } return pts; } function _nearAnyCharge(x, y, r = 18) { for (const c of charges) { const dx = x - c.x, dy = y - c.y; if (dx*dx + dy*dy < r*r) return true; } return false; } function drawEquipotentials() { if (charges.length === 0) return; const angularSkip = Math.max(1, Math.round(11 - equipDensity)); const seedsPerCharge = 24; const startRadius = 40; const color = "rgba(90, 190, 255, 0.45)"; const lineW = 0.9; ctx.strokeStyle = color; ctx.lineWidth = lineW; let drawnCount = 0, maxCurves = 120; for (const c of charges) { for (let i = 0; i < seedsPerCharge; i++) { if (i % angularSkip !== 0) continue; const ang = (i / seedsPerCharge) * 2 * Math.PI; const sx = c.x + startRadius * Math.cos(ang); const sy = c.y + startRadius * Math.sin(ang); if (_nearAnyCharge(sx, sy, 20)) continue; const step = equipDensity <= 3 ? 12 : equipDensity <= 6 ? 10 : 8; const maxSteps = 1400; const pts = traceEquipotential(sx, sy, step, maxSteps); if (pts.length < 6) continue; ctx.beginPath(); ctx.moveTo(pts[0].x, pts[0].y); for (let k = 1; k < pts.length; k++) ctx.lineTo(pts[k].x, pts[k].y); ctx.stroke(); drawnCount++; if (drawnCount >= maxCurves) return; } } } function drawPotentialMap() { const k = 8.99e9; const step = 6; const maxV = Math.max(1e3, 5e5 * charges.length / pixelsPerMeter); const alpha = 0.4; for (let y = 0; y < canvas.height; y += step) { for (let x = 0; x < canvas.width; x += step) { let V = 0; for (const c of charges) { const dx = (x - c.x) / pixelsPerMeter; const dy = (y - c.y) / pixelsPerMeter; const r = Math.sqrt(dx * dx + dy * dy); if (r < 1e-6) continue; const qC = c.q * (c.magnitudeC ?? defaultChargeMagnitudeC); V += (k * qC) / r; } const color = voltageToColor(V, maxV, alpha); ctx.fillStyle = color; ctx.fillRect(x, y, step, step); } } ctx.strokeStyle = "rgba(0, 255, 100)"; ctx.lineWidth = 1.8; for (let y = 0; y < canvas.height - step; y += step) { for (let x = 0; x < canvas.width - step; x += step) { const V00 = computePotential(x, y); const V10 = computePotential(x + step, y); const V01 = computePotential(x, y + step); const V11 = computePotential(x + step, y + step); const allPos = V00 > 0 && V10 > 0 && V01 > 0 && V11 > 0; const allNeg = V00 < 0 && V10 < 0 && V01 < 0 && V11 < 0; if (allPos || allNeg) continue; ctx.beginPath(); if ((V00 > 0) !== (V10 > 0)) { const t = Math.abs(V00) / (Math.abs(V00) + Math.abs(V10)); ctx.moveTo(x + t * step, y); } if ((V10 > 0) !== (V11 > 0)) { const t = Math.abs(V10) / (Math.abs(V10) + Math.abs(V11)); ctx.lineTo(x + step, y + t * step); } if ((V11 > 0) !== (V01 > 0)) { const t = Math.abs(V11) / (Math.abs(V11) + Math.abs(V01)); ctx.lineTo(x + (1 - t) * step, y + step); } if ((V01 > 0) !== (V00 > 0)) { const t = Math.abs(V01) / (Math.abs(V01) + Math.abs(V00)); ctx.lineTo(x, y + (1 - t) * step); } ctx.stroke(); } } } function voltageToColor(V, maxV, alpha = 0.4) { const clamp = Math.max(-maxV, Math.min(maxV, V)); const norm = clamp / maxV; const red = norm > 0 ? 255 * norm : 0; const blue = norm < 0 ? 255 * -norm : 0; const green = 60; return `rgba(${red.toFixed(0)},${green.toFixed(0)},${blue.toFixed(0)},${alpha})`; } const toggleFieldEl = document.getElementById("toggleField"); if (toggleFieldEl) { toggleFieldEl.checked = true; toggleFieldEl.addEventListener("change", (e) => { showField = e.target.checked; drawScene(); }); } const toggleEquipEl = document.getElementById("toggleEquip"); if (toggleEquipEl) { toggleEquipEl.checked = true; toggleEquipEl.addEventListener("change", (e) => { showEquip = e.target.checked; drawScene(); }); } const togglePotentialEl = document.getElementById("togglePotential"); if (togglePotentialEl) { togglePotentialEl.addEventListener("change", (e) => { showPotentialMap = e.target.checked; drawScene(); }); } const toggleChargeValuesEl = document.getElementById("toggleChargeValues"); if (toggleChargeValuesEl) { toggleChargeValuesEl.addEventListener("change", (e) => { showChargeValues = e.target.checked; drawScene(); }); } const pxInput = document.getElementById("pxPerMeterInput"); const scaleInfo = document.getElementById("scaleInfo"); const scaleError = document.getElementById("scaleError"); function applyScaleValue(val) { pixelsPerMeter = val; if (scaleInfo) scaleInfo.textContent = `${val} pixels = 1 meter`; drawScene(); } function validateAndApplyScale() { if (!pxInput) return; const raw = pxInput.value.trim(); const val = Number(raw); const valid = Number.isFinite(val) && Number.isInteger(val) && val >= 1 && val <= 500; if (valid) { pxInput.classList.remove("invalid"); if (scaleError) scaleError.style.display = "none"; applyScaleValue(val); } else { pxInput.classList.add("invalid"); if (scaleError) scaleError.style.display = "block"; } } if (pxInput) { pxInput.value = String(pixelsPerMeter); if (scaleInfo) scaleInfo.textContent = `${pixelsPerMeter} pixels = 1 meter`; pxInput.addEventListener("input", validateAndApplyScale); pxInput.addEventListener("change", validateAndApplyScale); pxInput.addEventListener("blur", validateAndApplyScale); } const fieldSlider = document.getElementById("fieldDensity"); const fieldValue = document.getElementById("fieldDensityVal"); if (fieldSlider && fieldValue) { fieldValue.textContent = String(fieldDensity); fieldSlider.value = String(fieldDensity); fieldSlider.addEventListener("input", (e) => { fieldDensity = Number(e.target.value); fieldValue.textContent = String(fieldDensity); drawScene(); }); } const densSlider = document.getElementById("equipDensity"); const densValue = document.getElementById("equipDensityVal"); if (densSlider && densValue) { densValue.textContent = String(equipDensity); densSlider.value = String(equipDensity); densSlider.addEventListener("input", (e) => { equipDensity = Number(e.target.value); densValue.textContent = String(equipDensity); drawScene(); }); } const chargeModal = document.getElementById("chargeModal"); const closeBtn = document.getElementById("closeChargeModal"); const saveBtn = document.getElementById("saveChargeBtn"); const titleEl = document.getElementById("chargeTitle"); const mantissaEl = document.getElementById("mantissaInput"); const exponentEl = document.getElementById("exponentInput"); const chargeErrEl = document.getElementById("chargeError"); let editingCharge = null; function openChargeModal(charge) { const tooltip = document.getElementById("chargeTooltip"); if (tooltip) tooltip.classList.add("hidden"); editingCharge = charge; const mag = charge.magnitudeC ?? defaultChargeMagnitudeC; const absMag = Math.abs(mag); let exponent = 0, mantissa = absMag; if (absMag > 0) { exponent = Math.floor(Math.log10(absMag)); mantissa = absMag / Math.pow(10, exponent); if (mantissa >= 10) { mantissa /= 10; exponent += 1; } } titleEl.textContent = `Edit charge (${charge.label})`; mantissaEl.value = mantissa.toString(); exponentEl.value = exponent.toString(); chargeErrEl.style.display = "none"; chargeModal.classList.remove("hidden"); chargeModal.setAttribute("aria-hidden", "false"); mantissaEl.focus(); } function closeChargeModal() { chargeModal.classList.add("hidden"); chargeModal.setAttribute("aria-hidden", "true"); editingCharge = null; } if (closeBtn) { closeBtn.addEventListener("click", closeChargeModal); } if (chargeModal) { chargeModal.addEventListener("click", (e) => { if (e.target === chargeModal) closeChargeModal(); }); } function saveChargeFromModal() { if (!editingCharge) return; const mant = Number(mantissaEl.value); const exp = Number(exponentEl.value); const validMant = Number.isFinite(mant) && mant > 0; const validExp = Number.isFinite(exp) && Number.isInteger(exp) && exp >= -12 && exp <= 12; if (!validMant || !validExp) { chargeErrEl.style.display = "block"; return; } editingCharge.magnitudeC = mant * Math.pow(10, exp); updateHudCharges(); drawScene(); closeChargeModal(); } function makeCharge(x, y, sign, magnitudeC = defaultChargeMagnitudeC) { const id = nextChargeId++; return { id, label: `q${id}`, x, y, q: sign, magnitudeC, }; } const scenarioNames = { singlePositive: "Single Positive Charge", singleNegative: "Single Negative Charge", dipole: "Electric Dipole", unequalDipole: "Unequal Dipole", twoPositive: "Two Positive Charges", threeLine: "Three Charges in a Line", quadrupole: "Quadrupole", triangle: "Charge Triangle", fourPositiveSquare: "Four Positive Charges", parallelPlates: "Parallel Plates" }; function loadScenario(name) { charges = []; nextChargeId = 1; const cx = canvas.width / 2; const cy = canvas.height / 2; if (name === "singlePositive") { charges.push(makeCharge(cx, cy, +1)); } if (name === "dipole") { charges.push(makeCharge(cx - 90, cy, +1)); charges.push(makeCharge(cx + 90, cy, -1)); } if (name === "twoPositive") { charges.push(makeCharge(cx - 90, cy, +1)); charges.push(makeCharge(cx + 90, cy, +1)); } if (name === "quadrupole") { charges.push(makeCharge(cx - 80, cy - 80, +1)); charges.push(makeCharge(cx + 80, cy - 80, -1)); charges.push(makeCharge(cx - 80, cy + 80, -1)); charges.push(makeCharge(cx + 80, cy + 80, +1)); } if (name === "parallelPlates") { const spacing = 35; for (let i = -4; i <= 4; i++) { charges.push(makeCharge(cx - 150, cy + i * spacing, +1, 5e-7)); charges.push(makeCharge(cx + 150, cy + i * spacing, -1, 5e-7)); } } if (name === "singleNegative") { charges.push(makeCharge(cx, cy, -1)); } if (name === "unequalDipole") { charges.push(makeCharge(cx - 90, cy, +1, 2e-6)); charges.push(makeCharge(cx + 90, cy, -1, 1e-6)); } if (name === "threeLine") { charges.push(makeCharge(cx - 120, cy, +1)); charges.push(makeCharge(cx, cy, -1)); charges.push(makeCharge(cx + 120, cy, +1)); } if (name === "triangle") { charges.push(makeCharge(cx, cy - 110, +1)); charges.push(makeCharge(cx - 100, cy + 80, -1)); charges.push(makeCharge(cx + 100, cy + 80, +1)); } if (name === "fourPositiveSquare") { charges.push(makeCharge(cx - 90, cy - 90, +1)); charges.push(makeCharge(cx + 90, cy - 90, +1)); charges.push(makeCharge(cx - 90, cy + 90, +1)); charges.push(makeCharge(cx + 90, cy + 90, +1)); } updateHudCharges(); updateSimStats(); const display = document.getElementById("currentScenarioDisplay"); if (display) { display.textContent = "Current Configuration: " + (scenarioNames[name] || "Custom"); } drawScene(); } const openScenariosBtn = document.getElementById("openScenariosBtn"); const closeScenariosBtn = document.getElementById("closeScenariosBtn"); const scenarioWindow = document.getElementById("scenarioWindow"); if (openScenariosBtn && scenarioWindow) { openScenariosBtn.addEventListener("click", () => { scenarioWindow.classList.remove("hidden"); }); } if (closeScenariosBtn && scenarioWindow) { closeScenariosBtn.addEventListener("click", () => { scenarioWindow.classList.add("hidden"); }); } if (scenarioWindow) { scenarioWindow.addEventListener("click", (e) => { if (e.target === scenarioWindow) { scenarioWindow.classList.add("hidden"); } }); } document.querySelectorAll(".scenarioBtn").forEach((btn) => { btn.addEventListener("click", () => { loadScenario(btn.dataset.scenario); if (scenarioWindow) scenarioWindow.classList.add("hidden"); }); }); const scenarioDescriptions = { singlePositive: "A single positive charge. Field lines point outward, and equipotential lines form circles around the charge.", singleNegative: "A single negative charge. Field lines point inward toward the charge, and equipotential lines form circles around it.", dipole: "A positive and negative charge of equal magnitude. Field lines travel from the positive charge to the negative charge.", unequalDipole: "A positive and negative charge with different magnitudes. The field pattern becomes asymmetric.", twoPositive: "Two equal positive charges. Field lines repel away from both charges, with a low-field region between them.", threeLine: "Three charges arranged in a line. This setup demonstrates superposition and field cancellation.", quadrupole: "Four alternating charges in a square-like pattern. This creates a more complex multipole field.", triangle: "Three charges arranged in a triangle. This shows how geometry affects electric field and potential patterns.", fourPositiveSquare: "Four positive charges at the corners of a square. The center has strong symmetry and field cancellation.", parallelPlates: "Two rows of opposite charges approximating parallel plates. The field between them is nearly uniform." }; const scenarioTooltip = document.createElement("div"); scenarioTooltip.id = "scenarioDescriptionTooltip"; scenarioTooltip.classList.add("hidden"); document.body.appendChild(scenarioTooltip); document.querySelectorAll(".scenarioBtn").forEach((btn) => { btn.addEventListener("mouseenter", () => { const key = btn.dataset.scenario; scenarioTooltip.textContent = scenarioDescriptions[key] || ""; scenarioTooltip.classList.remove("hidden"); }); btn.addEventListener("mousemove", (e) => { scenarioTooltip.style.left = `${e.clientX + 14}px`; scenarioTooltip.style.top = `${e.clientY + 14}px`; }); btn.addEventListener("mouseleave", () => { scenarioTooltip.classList.add("hidden"); }); }); const openInstructionsBtn = document.getElementById("openInstructionsBtn"); const closeInstructionsBtn = document.getElementById("closeInstructionsBtn"); const instructionsWindow = document.getElementById("instructionsWindow"); if (openInstructionsBtn && instructionsWindow) { openInstructionsBtn.addEventListener("click", () => { instructionsWindow.classList.remove("hidden"); }); } if (closeInstructionsBtn && instructionsWindow) { closeInstructionsBtn.addEventListener("click", () => { instructionsWindow.classList.add("hidden"); }); } if (instructionsWindow) { instructionsWindow.addEventListener("click", (e) => { if (e.target === instructionsWindow) { instructionsWindow.classList.add("hidden"); } }); } window.addEventListener("load", () => { if (instructionsWindow) { instructionsWindow.classList.remove("hidden"); } }); const openTogglesBtn = document.getElementById("openTogglesBtn"); const closeTogglesBtn = document.getElementById("closeTogglesBtn"); const togglesWindow = document.getElementById("togglesWindow"); if (openTogglesBtn && togglesWindow) { openTogglesBtn.addEventListener("click", () => { togglesWindow.classList.remove("hidden"); }); } if (closeTogglesBtn && togglesWindow) { closeTogglesBtn.addEventListener("click", () => { togglesWindow.classList.add("hidden"); }); } if (togglesWindow) { togglesWindow.addEventListener("click", (e) => { if (e.target === togglesWindow) { togglesWindow.classList.add("hidden"); } }); } if (saveBtn) { saveBtn.addEventListener("click", saveChargeFromModal); } const tooltip = document.getElementById("chargeTooltip"); canvas.addEventListener("mousemove", (e) => { const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; let found = null; for (const c of charges) { const dx = x - c.x; const dy = y - c.y; const dist = Math.hypot(dx, dy); if (dist < 20) { found = c; break; } } if (found) { const qC = found.q * (found.magnitudeC ?? defaultChargeMagnitudeC); const formattedQ = formatChargeValue(qC); const x_m = (found.x / pixelsPerMeter).toFixed(2); const y_m = (found.y / pixelsPerMeter).toFixed(2); tooltip.innerHTML = ` <b>${found.label}</b><br> Charge: ${formattedQ}<br> Position: (${x_m}, ${y_m}) m `; tooltip.style.left = `${e.clientX + 15}px`; tooltip.style.top = `${e.clientY + 15}px`; tooltip.classList.remove("hidden"); } else { tooltip.classList.add("hidden"); } }); const hud = document.getElementById("hud"); const hudDist = document.getElementById("hudDist"); const hudE = document.getElementById("hudE"); const hudV = document.getElementById("hudV"); canvas.addEventListener("mousemove", (e) => { if (!hud) return; const rect = canvas.getBoundingClientRect(); const x = e.clientX - rect.left; const y = e.clientY - rect.top; if (charges.length === 1 && hudDist) { const c = charges[0]; const dx_px = x - c.x; const dy_px = y - c.y; const d_px = Math.hypot(dx_px, dy_px); const d_m = d_px / pixelsPerMeter; const pxText = `${Math.round(d_px)} pixels`; const mText = d_m >= 1 ? `${d_m.toFixed(2)} m` : `${d_m.toExponential(2)} m`; hudDist.textContent = `Distance: ${pxText}, ${mText}`; } else if (hudDist) { hudDist.textContent = ""; } if (charges.length === 0) { if (hudE) hudE.textContent = "E: —"; if (hudV) hudV.textContent = "V: —"; return; } const V = computePotential(x, y); let vTxt; if (Math.abs(V) >= 1e6) { vTxt = `${(V / 1e6).toFixed(2)} ×10^6 V`; } else if (Math.abs(V) >= 1e3) { vTxt = `${(V / 1e3).toFixed(2)} ×10^3 V`; } else if (Math.abs(V) >= 1) { vTxt = `${V.toFixed(2)} V`; } else { vTxt = `${V.toExponential(2)} V`; } if (hudV) hudV.textContent = `V: ${vTxt}`; const { Ex, Ey } = computeElectricField(x, y); const Emag = Math.hypot(Ex, Ey); let txt; if (Emag >= 1e6) { txt = `${(Emag / 1e6).toFixed(2)} ×10^6 N/C`; } else if (Emag >= 1e3) { txt = `${(Emag / 1e3).toFixed(2)} ×10^3 N/C`; } else if (Emag >= 1) { txt = `${Emag.toFixed(2)} N/C`; } else { txt = `${Emag.toExponential(2)} N/C`; } if (hudE) hudE.textContent = `E: ${txt}`; }); canvas.addEventListener("mouseleave", () => { if (hudDist) hudDist.textContent = ""; if (hudE) hudE.textContent = "E: —"; if (hudV) hudV.textContent = "V: —"; }); drawScene(); updateSimStats();